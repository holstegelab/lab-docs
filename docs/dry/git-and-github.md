# Git and GitHub

Basic workflow:

```bash
git clone repo
git add file
git commit -m "message"
git push
```

Rules:

- Always use `.gitignore`.
- Do not commit raw sequencing data.
- Do not commit large binary artifacts.

Ask for review or help when changing shared project history or repo structure.
